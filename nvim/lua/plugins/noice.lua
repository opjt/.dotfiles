return {
  "folke/noice.nvim",
  opts = {
    presets = {
      command_palette = false,
    },
    cmdline = {
      view = 'cmdline', -- classic bottom cmdline instead of the floating popup
    },
  },
}
